# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, Tab, exec, $terminal
bind = $mainMod, Z, killactive,
bind = $mainMod, Q, exit,
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, F, togglefloating,
bind = $SUPER_SHIFT, F, fullscreen
bind = $mainMod, Space, exec, $menu
#bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

bind = $mainMod, W, exec, linux-wallpaperengine --silent --fps 60 --scaling fill --screen-root DP-1 --screen-root DP-2 --screen-root DP-3 1480760695

# Emoji selector
bind = $mainMod ALT, Space, exec, $emojis

# Audio settings
bind = $mainMod, S, exec, pwvucontrol

# Web Browser
bind = $mainMod, B, exec, zen-browser

# Power Button/PowerMenu
bind = , XF86PowerOff, exec, $sessionMenu          #~/scripts/wlogout.sh  #~/.config/rofi/powermenu/type-5/powermenu.sh
bind = $mainMod, p, exec, $sessionMenu             #~/scripts/wlogout.sh  #~/.config/rofi/powermenu/type-5/powermenu.sh

# Screenshot
bind = , PRINT, exec, hyprshot -m window
bind = SHIFT, PRINT, exec, hyprshot -m region

# Manual lock
bind = $SUPER_SHIFT, l, exec, hyprlock

# Audio control
bind = $mainMod, D, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%+
bind = $mainMod, A, exec, wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 5%-

# Brightness control
bind = $SUPER_SHIFT, D, exec, brightnessctl s 10%+
bind = $SUPER_SHIFT, A, exec, brightnessctl s 10%-
#bind = $SUPER_SHIFT, A, exec, ~/scripts/brightness.sh 
#bind = $SUPER_SHIFT, D, exec, ~/scripts/brightness.sh



# Reload waybar // Toggle Quickshell
bind = $mainMod, R, exec, $ToggleBar 
#~/scripts/waybar-rld.sh

# Show/Hide nwg-dock
#bind = $mainMod, X, exec, nwg-dock-hyprland -r -i 35 -w 10 -mb 2 -ml 10 -mr 10 -x

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, j, movefocus, d
bind = $mainMod, k, movefocus, u
bind = $mainMod, l, movefocus, r

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
#bind = $mainMod, mouse_down, workspace, e+1
#bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Cursor Zoom
#bind = $mainMod ALT, mouse_down, exec, hyprctl keyword misc:cursor_zoom_factor  x = ( x + 0.5)
#bind = $mainMod ALT, mouse_up, exec, hyprctl keyword misc:cursor_zoom_factor x = (x - 0.5)
bind = $mainMod ALT, mouse_up, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') - 2.0}")
bind = $mainMod ALT, mouse_down, exec, hyprctl keyword cursor:zoom_factor $(awk "BEGIN {print $(hyprctl getoption cursor:zoom_factor | grep 'float:' | awk '{print $2}') + 2.0}")

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

